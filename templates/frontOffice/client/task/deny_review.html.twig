<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deny Task Review</title>
    <link rel="stylesheet" href="{{ asset('css/auth.css') }}">
    <link rel="stylesheet" href="{{ asset('css/dashboard.css') }}">
    <style>
        body { background: #f4f7fb; margin: 0; font-family: Arial, sans-serif; }
        .page { max-width: 780px; margin: 40px auto; background: #fff; border-radius: 18px; padding: 28px; box-shadow: 0 14px 40px rgba(15, 23, 42, 0.08); }
        .title { margin: 0 0 8px; font-size: 28px; font-weight: 800; color: #0f172a; }
        .subtitle { margin: 0 0 20px; color: #475569; }
        .actions { display: flex; gap: 12px; margin-top: 18px; }
        .btn { display: inline-flex; align-items: center; justify-content: center; min-height: 46px; padding: 0 18px; border-radius: 12px; text-decoration: none; font-weight: 700; border: none; cursor: pointer; }
        .btn-primary { background: #dc2626; color: #fff; }
        .btn-secondary { background: #e2e8f0; color: #0f172a; }
        .error { color: #dc2626; margin-bottom: 14px; }
    </style>
</head>
<body>
    <div class="page">
        <h1 class="title">Deny Task Review</h1>
        <p class="subtitle">Task: {{ task.title }}</p>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            <div class="error">{{ form_errors(form) }}</div>
            {{ form_row(form.clientFeedback) }}

            <div class="actions">
                <button type="submit" class="btn btn-primary">Deny Task</button>
                <a href="{{ path('client_project_index') }}" class="btn btn-secondary">Cancel</a>
            </div>
        {{ form_end(form) }}
    </div>
</body>
</html>
