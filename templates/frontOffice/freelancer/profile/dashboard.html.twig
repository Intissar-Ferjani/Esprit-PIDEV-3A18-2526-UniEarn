{# templates/frontOffice/freelancer/dashboard.html.twig #}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniEarn – Freelancer Dashboard</title>
    <link rel="stylesheet" href="{{ asset('css/auth.css') }}">
    <link rel="stylesheet" href="{{ asset('css/dashboard.css') }}">
</head>
<body class="dashboard-body">

<div class="dashboard-layout">

    {% include 'frontOffice/freelancer/profile/_sidebar.html.twig' with {'active': 'dashboard'} %}

    <div class="dashboard-main">

        <div class="top-bar">
            <span class="top-bar-title">Dashboard</span>
            <div class="top-bar-right">
                <span class="top-bar-user">👤 {{ user.name }}</span>
            </div>
        </div>

        <div class="dashboard-content">

            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="flash flash-{{ label }}">{{ message }}</div>
                {% endfor %}
            {% endfor %}

            {# ── Profile Header Card ── #}
            <div class="profile-header-card">
                <div class="profile-banner"></div>
                <div class="profile-header-body">

                    {# Avatar #}
                    <div class="avatar-wrap">
                        <form method="post" action="{{ path('freelancer_change_photo') }}" enctype="multipart/form-data" id="photoForm">
                            <label for="photoInput" class="avatar-label" title="Change photo">
                                {% if user.profilePicturePath %}
                                    <img src="{{ asset(user.profilePicturePath) }}" class="profile-avatar" alt="Avatar">
                                {% else %}
                                    <img src="{{ asset('images/default-avatar.jpg') }}" class="profile-avatar" alt="Avatar">
                                {% endif %}
                                <span class="avatar-camera">📷</span>
                            </label>
                            <input type="file" id="photoInput" name="profile_photo" accept="image/*"
                                   style="display:none" onchange="document.getElementById('photoForm').submit()">
                        </form>
                    </div>

                    {# Info #}
                    <div class="profile-info">
                        <h2 class="profile-name">{{ user.name }}</h2>
                        <p class="profile-email">{{ user.email }}</p>
                        <div class="profile-pills">
                            <span class="pill pill-blue">💰 {{ freelancer.pricePerHour }} TND/hr</span>
                            <span class="pill pill-orange">⭐ {{ freelancer.rating }}</span>
                            {% if freelancer.verificationStatus == 'verified' %}
                                <span class="pill pill-green">✅ Verified</span>
                            {% else %}
                                <span class="pill" style="background:#fef3c7;color:#b45309;">⏳ Not Verified</span>
                            {% endif %}
                        </div>

                        {# Skills #}
                        <div class="skills-wrap" style="margin-top:10px;display:flex;flex-wrap:wrap;gap:6px;">
                            {% for skill in freelancer.skillsArray %}
                                <span class="pill pill-blue" style="font-size:11px;">{{ skill }}</span>
                            {% endfor %}
                        </div>
                    </div>

                    {# Edit button #}
                    <div class="profile-edit-btn-wrap">
                        <a href="{{ path('freelancer_profile_edit') }}" class="btn-edit">✏️ Edit Profile</a>
                    </div>
                </div>
            </div>

            {# ── About Me ── #}
            <div class="content-section" style="margin-top:20px;">
                <div class="section-header">
                    <div class="section-title-wrap">
                        <span class="section-icon">👤</span>
                        <h3 class="section-title">About Me</h3>
                    </div>
                </div>
                <hr class="section-divider">
                <p style="color:#374151;font-size:14px;line-height:1.7;">
                    {{ freelancer.bio ?: 'No bio available.' }}
                </p>
            </div>

            {# ── Portfolio Section ── #}
            <div class="content-section" style="margin-top:20px;">
                <div class="section-header">
                    <div class="section-title-wrap">
                        <span class="section-icon">💼</span>
                        <h3 class="section-title">Portfolio</h3>
                    </div>
                </div>
                <hr class="section-divider">

                <div style="text-align:center;padding:30px 0;">
                    <div style="font-size:48px;margin-bottom:12px;">💼</div>
                    <p style="font-weight:700;font-size:17px;color:#1e293b;margin-bottom:6px;">Showcase Your Work</p>
                    <p style="color:#718096;font-size:13px;margin-bottom:20px;">Build your portfolio to attract more clients</p>
                    <a href="{{ path('freelancer_portfolio_page') }}" class="btn-edit">
                        View My Portfolio →
                    </a>
                </div>
            </div>

        </div>
    </div>
</div>

</body>
</html>
